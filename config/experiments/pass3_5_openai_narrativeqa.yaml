# Pass 3.5 A0 validation: OpenAI-only NarrativeQA, 4 bins, single budget [1.0].
# Small, rate-limit-friendly run with no KV compression (budget 1.0 only), with a prepare-time prompt-length cap.
#
# Usage (uv):
#   uv run python -m kv_transition all -c config/experiments/pass3_5_openai_narrativeqa.yaml

dataset:
  name: longbench
  task: narrativeqa
  n_per_bin: 6
  max_prompt_tokens: 7000
  seed: 1337

binning:
  n_bins: 4

kv:
  policy: none
  budgets: [1.0]

decoding:
  temperature: 0
  max_tokens: 256

output:
  exp_group_id: pass3_5_openai_narrativeqa_v1

db:
  path: auto


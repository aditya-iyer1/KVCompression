# Copy to .env and fill values (do NOT commit real secrets).
# Use with uv: `uv run ...` (uv will load .env if your environment/setup does so, or export vars in your shell).

# OpenAI-compatible API key (OpenAI or any compatible provider)
OPENAI_API_KEY=

# Base URL for OpenAI-compatible server.
# Examples:
# - OpenAI: https://api.openai.com/v1
# - Local vLLM OpenAI server: http://localhost:8000/v1
# - SGLang OpenAI server: http://localhost:30000/v1
BASE_URL=

# Model name as expected by the endpoint (examples: gpt-4.1-mini, gpt-4o-mini, llama-3.1-8b-instruct, etc.)
MODEL_NAME=

# Optional: if your setup uses a different env var name for keys, you can point config.engine.api_key_env to it.
# Example:
# MY_CUSTOM_API_KEY=